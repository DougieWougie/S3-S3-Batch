[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    unit: Unit tests (moto-based mocking)
    integration: Integration tests (requires AWS credentials)
addopts = -v --tb=short
